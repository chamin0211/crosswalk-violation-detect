# crosswalk-violation-detect 인수인계 (2026-09-06 → 09-07)

새 대화창 시작 시 이 문서를 먼저 읽고 진행한다.

---

## 0. 프로젝트 한 줄 요약

비신호 횡단보도에서 도로교통법 제27조(보행자 보호의무) 위반 후보를 탐지해 경찰 검토를 지원하는 Streamlit 웹앱. YOLO11로 사람·차량을 추적하고, 직접 설계한 규칙 기반 로직으로 판정한다.

- 경로: `~/crosswalk-violation-detect/`
- 실행: `uv run streamlit run app.py`
- 저장소: `git@github.com:chamin0211/crosswalk-violation-detect.git` (SSH, 2026-09-06 커밋 `6de542f`까지 푸시 완료)

---

## 1. 오늘(09-06) 한 일

### 1-1. PCPA 완전 제거 (완료)

**결론: PCPA는 차량 탑재 카메라 전용 모델이라 고정 CCTV에 부적합. 제거함.**

진단 과정에서 확인한 것:

| 검증 | 결과 |
|---|---|
| 모델 로딩, npz 읽기, 입력 순서 | 정상 |
| 노이즈 테스트 (가짜 입력 3종) | 0.41 vs 0.99999 → 모델은 입력에 반응함 |
| 델타 인코딩(`normalize_boxes`) 가설 | **배제 확정** |
| pose 관절 순서 | **불일치** (학습=OpenPose, 우리=COCO-17) |
| pose 값 스케일 | **불일치** (학습=0~1 정규화, 우리=픽셀 좌표) |
| speed 입력 | **도메인 불일치** (PIE는 차량 OBD 속도, 우리는 고정 CCTV라 값 없음) |

델타 인코딩 배제 근거 (다시 제안하지 말 것):
- 모델 입력 shape이 전부 시간축 16 (`input_1`, `input_pose`, `input_box`, `input_speed`)
- `action_predict.py:4791-4792`, `4855-4856`에서 PCPA 클래스가 `model_opts['normalize_boxes'] = False`로 강제 덮어씀

**제거 작업 (전부 완료, 실행 확인됨):**
- `determine_violation(result)` — `person_probabilities`, `prob_threshold` 인자 제거
- `render_annotated_video(video_path, result, is_violation, output_path=...)` — 확률 인자 및 화면 표시 제거
- `app.py` — subprocess 호출, `import json`, `import subprocess`, `PCPA_PYTHON`/`PCPA_SCRIPT` 상수 제거
- `pipeline_core.py` — npz 저장 블록, `output_npz` 인자, crop 수집 전체 제거
- `"frames"` 값을 `len(d["crop"])`(32 상한) → `len(d["frames"])`(실제 프레임 수)로 수정

**부수 효과:** conda `intention_env`, 1.8GB 체크포인트, npz subprocess 통신이 전부 불필요해짐. 새 환경 세팅이 `uv sync` 하나로 끝난다.

### 1-2. 검증 결과 (오늘 처음 확인한 것들)

| 항목 | 결과 |
|---|---|
| 위반 탐지 | 통과 (여러 영상에서 확인) |
| **정지 판정** | **통과** — 브레이크 등 켜고 멈춘 차를 위반으로 잡지 않음 (`right_road.mp4` 5초 지점) |
| 오탐 (보행자 없을 때) | 통과 — 위반 0건 |
| 해상도 일반화 | 통과 — 1404×1014, 1760×1440 모두 작동 |
| 재현성 | 통과 — 3회 실행 시 판정 동일 (세부 수치는 ±0.06 변동) |

**트랙 조각화 실태:** 실제 보행자 2명이 6개 ID로 쪼개지고 ID 스왑까지 발생(5→17→18, 6→12→18→21). 그럼에도 판정 결과는 3회 모두 동일. 프레임 단위 교집합 판정이 ID 정합성에 의존하지 않기 때문. 법 요건도 개인 식별을 요구하지 않으므로 이 상태를 허용한다.

### 1-3. 오토바이 검출 문제 (미해결 → 내일 작업)

**측정 결과 (`right_road.mp4` 60~80초, 650프레임, conf 0.05로 전수 스캔):**

| 클래스 | 중앙값 | conf 0.35 이상 |
|---|---|---|
| car | **0.692** | 58.6% |
| person | 0.288 | 45.9% |
| **motorcycle** | **0.151** | **18.6%** |

오토바이 신뢰도가 자동차의 5분의 1.

**yolo11l 비교 실험 (같은 영상):**

| 항목 | yolo11m | yolo11l | 판정 |
|---|---|---|---|
| motorcycle 중앙값 | 0.151 | 0.216 (+43%) | 개선 |
| **car 중앙값** | **0.692** | **0.141 (-80%)** | **악화** |
| car 검출 수 | 2952 | 5559 (+88%) | 중복 검출 증가 |

→ **모델 교체는 배제.** 오토바이를 살리려다 자동차를 망친다.

**시각 확인 결과 (`check_moto_visual.py`, conf 0.15):**
- 낮은 conf에서 잡힌 박스는 **전부 실제 오토바이 위에 정확히 있음** (오탐 아님)
- 그러나 신뢰도가 프레임 간 극심하게 요동 (75.5초 0.16 → 76.0초 0.62)
- 같은 오토바이에 중복 박스 발생 (NMS가 못 합침)
- 폴리곤 근처를 지나간 오토바이는 conf 0.15로도 미검출 구간 존재 (65~66초)

**병목이 세 겹:**
```
pipeline_core.py:  conf=0.35
my_bytetrack.yaml: track_high_thresh: 0.35
my_bytetrack.yaml: new_track_thresh: 0.45   ← 가장 높음
```
새 트랙 생성에 0.45가 필요한데 오토바이 중앙값이 0.151이라 트랙이 아예 안 만들어진다. 그래서 차량 기록이 비었던 것.

**임계값 조정만으로는 불충분:** conf 0.20으로 내려도 39%만 검출. 검출이 끊겨서 안정적 추적 불가.

---

## 2. 내일(09-07) 작업: 오토바이 파인튜닝

### 2-1. 목표

커스텀 데이터로 YOLO11을 파인튜닝해 오토바이 검출 신뢰도를 올린다. 목표는 중앙값 0.151 → 0.5 이상.

### 2-2. 데이터 현황

- 당수cctv2 영상에 오토바이 **13대** 지나감 (육안 확인)
- 다른 영상(당수 긴, 긴3, 긴4 등)에도 있을 것으로 추정
- Roboflow 계정 보유

### 2-3. 시간 계획 (7시간)

| 시간 | 작업 |
|---|---|
| 1시간 | 프레임 추출 + 사전 라벨링 생성 |
| 3시간 | Roboflow에서 라벨 확인·수정 |
| 1시간 | 학습 |
| 1시간 | 검증 및 파이프라인 통합 |
| 1시간 | 문서 정리 |

### 2-4. 1단계: 프레임 추출 + 사전 라벨링

**핵심 아이디어:** conf 0.10으로 검출한 박스를 라벨 초안으로 저장한다. 오늘 확인한 대로 낮은 conf 박스도 위치는 정확하므로, 라벨링이 "처음부터 그리기"가 아니라 "확인하고 고치기"가 되어 시간이 절반 이하로 준다.

`~/crosswalk-violation-detect/extract_moto.py`:

```python
"""
오토바이가 나오는 프레임을 자동으로 찾아 추출하고 사전 라벨을 만든다.
- conf를 낮춰(0.10) 검출한 뒤 그 박스를 라벨 초안으로 저장
- 연속 프레임은 거의 같은 이미지이므로 간격을 두고 샘플링
출력:
  data/moto_dataset/images/*.jpg
  data/moto_dataset/labels/*.txt   (YOLO 형식: class cx cy w h, 0~1 정규화)
"""
import os
import cv2
from ultralytics import YOLO

VIDEO = "data/raw/dangsu2.mp4"      # 처리할 영상 (파일명 확인 필요)
OUT_DIR = "data/moto_dataset"
DETECT_CONF = 0.10
SAMPLE_STRIDE = 5                    # 검출된 프레임 중 N개마다 1장 저장
IMGSZ = 1920
MOTO_CLASS = 3                       # COCO motorcycle

img_dir = os.path.join(OUT_DIR, "images")
lbl_dir = os.path.join(OUT_DIR, "labels")
os.makedirs(img_dir, exist_ok=True)
os.makedirs(lbl_dir, exist_ok=True)

model = YOLO("yolo11m.pt")
cap = cv2.VideoCapture(VIDEO)
fps = cap.get(cv2.CAP_PROP_FPS)
total = int(cap.get(cv2.CAP_PROP_FRAME_COUNT))
video_name = os.path.splitext(os.path.basename(VIDEO))[0]

print("영상:", VIDEO)
print("총 프레임:", total, "/ fps:", fps)
print("=" * 60)

frame_idx = 0
hit_count = 0
saved_count = 0
segments = []
last_hit_frame = -999

while True:
    ok, frame = cap.read()
    if not ok:
        break

    r = model.predict(frame, device=0, conf=DETECT_CONF, imgsz=IMGSZ,
                      classes=[MOTO_CLASS], verbose=False)[0]

    if len(r.boxes) > 0:
        if frame_idx - last_hit_frame > 30:
            segments.append([frame_idx, frame_idx])
        else:
            segments[-1][1] = frame_idx
        last_hit_frame = frame_idx

        if hit_count % SAMPLE_STRIDE == 0:
            h, w = frame.shape[:2]
            base = "{}_f{:06d}".format(video_name, frame_idx)

            cv2.imwrite(os.path.join(img_dir, base + ".jpg"), frame)

            lines = []
            for i in range(len(r.boxes)):
                x1, y1, x2, y2 = r.boxes.xyxy[i].tolist()
                cx = ((x1 + x2) / 2) / w
                cy = ((y1 + y2) / 2) / h
                bw = (x2 - x1) / w
                bh = (y2 - y1) / h
                lines.append("0 {:.6f} {:.6f} {:.6f} {:.6f}".format(cx, cy, bw, bh))

            with open(os.path.join(lbl_dir, base + ".txt"), "w") as f:
                f.write("\n".join(lines) + "\n")

            saved_count += 1

        hit_count += 1

    frame_idx += 1
    if frame_idx % 300 == 0:
        print("  진행: {}/{}  (저장 {}장)".format(frame_idx, total, saved_count))

cap.release()

print("=" * 60)
print("오토바이 검출 프레임:", hit_count)
print("저장한 이미지:", saved_count, "장")
print("=" * 60)
print("등장 구간 (초 단위)")
print("=" * 60)
for i in range(len(segments)):
    s, e = segments[i]
    print("  {:2d}번: {:6.1f}s ~ {:6.1f}s  ({}프레임)".format(
        i + 1, s / fps, e / fps, e - s + 1))
print("=" * 60)
print("총", len(segments), "개 구간")
print("저장 위치:", img_dir)
```

실행:
```bash
cd ~/crosswalk-violation-detect
uv run python extract_moto.py
```

**결과 확인 기준:**
- 등장 구간 수가 13에 가까우면 검출 양호. 5~6개면 `DETECT_CONF`를 0.05로 낮춘다.
- 저장 장수가 500 넘으면 `SAMPLE_STRIDE`를 10으로, 100 미만이면 3으로 조정.

**주의:** 이 스크립트는 검출된 프레임만 저장한다. **완전히 놓친 오토바이는 데이터셋에 안 들어간다.** 등장 구간 목록과 육안 확인(13대)을 대조해서, 빠진 구간이 있으면 그 구간 프레임을 수동으로 추가해야 한다. 놓친 케이스야말로 학습에 가장 필요한 데이터다.

### 2-5. 2단계: Roboflow 라벨링

1. Roboflow에서 새 프로젝트 생성 (Object Detection, 클래스 1개: `motorcycle`)
2. `data/moto_dataset/images/`와 `labels/`를 함께 업로드 (YOLO 형식 자동 인식)
3. 사전 라벨이 이미 그려진 상태로 열린다. 할 일:
   - 박스 위치가 어긋난 것 수정
   - 중복 박스 삭제 (한 오토바이에 하나만)
   - 검출이 놓친 오토바이 새로 그리기
   - 오토바이가 아닌 오탐 삭제
4. Train/Valid/Test 분할 (권장 70/20/10)
5. Augmentation은 최소한으로 (야간 CCTV 특성상 밝기/대비 변형은 도움될 수 있으나, 회전·좌우반전은 카메라가 고정이므로 부적절)
6. YOLOv11 형식으로 Export → 다운로드

**다양성 확보가 양보다 중요하다.** 13대가 전부 같은 방향·같은 거리로 지나가면 학습 효과가 제한적이다. 가능하면 다른 영상에서도 추출해 방향·거리·조명·가림 상황을 섞는다.

### 2-6. 3단계: 학습

Roboflow에서 받은 zip을 풀면 `data.yaml`이 들어 있다. 경로만 확인·수정한다.

`~/crosswalk-violation-detect/train_moto.py`:

```python
"""
오토바이 검출 파인튜닝.
단일 클래스(motorcycle)로 학습하므로, 결과 모델은 오토바이만 검출한다.
기존 yolo11m(사람·차량)과 병행 사용하는 구조가 된다.
"""
from ultralytics import YOLO

model = YOLO("yolo11m.pt")

model.train(
    data="data/moto_dataset/data.yaml",   # Roboflow export 경로로 수정
    epochs=80,
    imgsz=1280,          # 1920은 메모리 부족 가능. 부족하면 960으로
    batch=4,             # RTX 4060/3060Ti 8GB 기준. OOM 나면 2로
    device=0,
    patience=20,         # 20에폭 개선 없으면 조기 종료
    project="runs/moto",
    name="finetune_v1",
)
```

실행:
```bash
cd ~/crosswalk-violation-detect
uv run python train_moto.py
```

**메모리 주의:** `imgsz=1920`, `batch=4`는 8GB에서 OOM 날 가능성이 높다. `imgsz=1280`, `batch=4`로 시작하고, 실패하면 batch를 2로 낮춘다. 학습 시 imgsz와 추론 시 imgsz가 다르면 성능이 떨어지므로, 최종적으로 파이프라인의 추론 imgsz도 맞춰야 한다.

학습 결과는 `runs/moto/finetune_v1/weights/best.pt`에 저장된다.

### 2-7. 4단계: 검증

학습 전후를 같은 기준으로 비교한다. `measure_conf.py`가 이미 있으므로 모델 경로만 바꿔서 돌린다.

```bash
cd ~/crosswalk-violation-detect
sed -i 's|YOLO("yolo11m.pt")|YOLO("runs/moto/finetune_v1/weights/best.pt")|' measure_conf.py
uv run python measure_conf.py
```

**비교 기준 (`moto_20s.mp4` 650프레임, yolo11m 기준선):**

| 항목 | 학습 전 | 목표 |
|---|---|---|
| motorcycle 중앙값 | 0.151 | 0.50 이상 |
| motorcycle conf 0.35 이상 | 18.6% | 70% 이상 |
| motorcycle conf 0.45 이상 | (미측정) | 50% 이상 |

`new_track_thresh: 0.45`를 통과해야 트랙이 만들어지므로, **0.45 이상 비율이 실질적인 성공 지표**다.

시각 확인도 병행한다. `check_moto_visual.py`에서 모델 경로와 `CONF`를 바꿔 돌리고, 박스가 안정적으로 붙어 있는지(깜빡이지 않는지) 본다.

### 2-8. 5단계: 파이프라인 통합

**여기가 설계 판단이 필요한 지점이다. 서둘러 붙이지 말 것.**

단일 클래스로 파인튜닝하면 결과 모델은 오토바이만 검출한다. 기존 `yolo11m`(사람·차량)과 병행해야 하는데, 방법이 몇 가지 있고 각각 문제가 있다.

**방법 A: 두 번째 `track()` 호출 후 병합**
- `pipeline_core.py:422` 근처에서 오토바이 전용 모델로 `track()`을 한 번 더 호출
- track ID가 충돌하므로 오프셋 필요 (예: `track_id + 10000`)
- 결과를 `vehicle_tracks`에 병합
- **비용:** 프레임당 추론이 2회 → 3회로 늘어 처리 시간 약 50% 증가. 현재 80초 영상에 10분 걸리므로 15분이 된다.

**방법 B: 전체 클래스 파인튜닝**
- 사람·차량까지 다 라벨링해야 함. 시간상 불가능.

**방법 C: 오토바이 검출만 별도 후처리**
- 오토바이 트랙을 따로 만들어 `determine_violation`에 합류시킴
- 추적 품질이 떨어질 수 있음

**방법 A가 현실적이지만, 처리 시간 증가를 감수할지 판단이 필요하다.** 통합 전에 4단계 검증에서 실제로 개선됐는지부터 확인하고, 개선이 미미하면 통합하지 않는 편이 낫다.

### 2-9. 실패 시 결론

파인튜닝이 목표(중앙값 0.5)에 못 미치면 오토바이는 향후 과제로 남긴다. 이미 근거는 충분히 쌓였다:

> 오토바이 검출 신뢰도가 자동차의 5분의 1(0.151 vs 0.692)로 측정되었다. 더 큰 모델(yolo11l)은 오토바이를 43% 개선하는 대신 자동차 검출을 80% 악화시켜 배제했다. 임계값을 0.20으로 낮춰도 39%만 검출되며, 프레임 간 신뢰도가 0.16~0.62로 요동쳐 안정적 추적이 불가능했다. 커스텀 데이터 파인튜닝을 시도했으나 [결과]. 현재 버전은 자동차 중심으로 판정한다.

이는 "못 했다"가 아니라 "측정하고 판단했다"는 서술이다.

---

## 3. 남은 작업 (오토바이 외)

우선순위 순.

### 3-1. CSV 내보내기 (미착수, 약 2시간)

SETUP.md에 최종 결과물로 명시했으나 아직 구현되지 않았다. 컬럼은 이미 확정했다.

| 컬럼 | 예시 | 출처 |
|---|---|---|
| 위반번호 | 1 | 순번 |
| 발생시각(초) | 6.9 | `시각(초)[0]` |
| 종료시각(초) | 9.6 | `시각(초)[1]` |
| 지속시간(초) | 1.5 | 계산 |
| 보행자상태 | 접근 중 / 횡단 중 | `person_states` 한글 변환 |
| 차량상태 | 정지하지 않고 통과 | `vehicle_motion_states` 한글 변환 |
| 최대정지시간(초) | 0.07 | 차량 데이터 `max_stop_duration_sec` |
| 횡단보도침범비율 | 76% | 차량 데이터 `max_overlap` |
| 판정등급 | 위반 가능성 높음 | `severity` 한글 변환 |
| 진입스냅샷 | violation_0_enter.jpg | `snapshots.enter` |
| 통과스냅샷 | violation_0_pass.jpg | `snapshots.pass` |
| 추적번호(참고용) | 10 | `vehicle_track_ids[0]` |

결정 사항:
- 파일명은 `violations.csv` 고정 (녹화 영상이라 정확한 촬영 시각을 알 수 없어 타임스탬프를 붙이지 않음)
- 시각은 영상 파일 기준 상대 시간만 (실제 CCTV 연동 시 절대 시각 컬럼 추가)
- `person_track_ids`는 제외 (실제 사람과 대응되지 않아 오해 소지)
- 차량 ID는 "추적번호(참고용)"으로 맨 뒤에 배치 (개발자 탭 대조용, 실차 식별용 아님을 이름으로 명시)

다운로드 버튼 위치를 정하려면 `app.py`의 탭 구조 확인 필요:
```bash
grep -n "st.tabs\|tab1\|위반 후보" ~/crosswalk-violation-detect/app.py | head -15
```

### 3-2. UI 문구 개선 (약 1시간)

현재 화면이 개발자용이라 검토자(경찰)가 이해하기 어렵다는 문제 제기가 있었다.

구체적 문제:
- "최근접 거리 1.87"이 무슨 단위인지 불명확 (미터로 오해 가능). 음수가 나오는 이유도 설명 없음
- "침범 정도 76%"의 의미 불명확
- 차량 1은 침범 100%인데 위반이 아님 → 그 시간대에 보행자가 없었기 때문인데 화면에 설명 없음
- 보행자 기록에 9줄이 나오는데 실제로는 2명 (트랙 조각화)

개선 방향:
- 탭 이름과 캡션 보강 ("차량 기록" → "차량별 통과 기록" 등)
- 판정 대상이 아닌 보행자는 기본 숨김, "전체 보기" 토글 제공
- 개발자 탭은 접어두기 (이름이 "개발자"인 것은 유지)

### 3-3. SETUP.md 갱신 (약 30분)

PCPA 제거로 문서 절반이 무효화됐다. 지금 그대로 따라 하면 불필요한 conda 환경과 1.8GB 체크포인트를 받느라 시간을 낭비한다.

삭제할 절: 4장(PCPA 환경 구성) 전체, 5장 일부(PCPA 경로 상수), 6장 일부(PCPA CPU 강제), 8장 체크리스트의 PCPA 항목

새 세팅 절차는 이게 전부다:
```bash
git clone git@github.com:chamin0211/crosswalk-violation-detect.git
cd crosswalk-violation-detect
uv sync
uv run python -c "import torch; print(torch.cuda.is_available())"   # True 확인
which ffmpeg    # pipeline_core.py의 절대경로와 일치하는지 확인
uv run streamlit run app.py
```

갱신할 내용: 알려진 한계에 오토바이 검출 문제 추가, 처리 속도 실측치 반영(실시간 대비 약 5배, 80초 영상에 10분)

### 3-4. DECISIONS.md 갱신 (약 30분)

PCPA 섹션은 09-06 세션 중 갱신본을 만들어뒀다. 여기에 추가할 것:
- 오토바이 검출 측정 결과와 yolo11l 배제 근거
- 정지 판정 검증 통과 (첫 확인)
- 해상도 일반화 검증
- 재현성 검증 (3회 실행)
- 트랙 조각화 허용 결정 (ID 정합성은 법 요건이 아님)

### 3-5. 발표 자료 (미착수)

DECISIONS.md의 시행착오 기록이 그대로 발표 재료다. 잘 된 것만 보여주는 발표보다 판단 근거를 보여주는 편이 낫다.

핵심 서술:
- YOLO11 검출·추적·자세추정 위에 도로교통법 제27조 요건을 반영한 규칙 기반 판정 로직을 결합
- PCPA(딥러닝 횡단의도 예측) 적용 과정에서 차량 탑재 카메라 전제(OBD 속도 입력, 운전자 시점)를 발견하고 제외
- 인식은 딥러닝, 판단은 규칙으로 분리 — 단속 시스템은 판단 근거를 설명할 수 있어야 하므로

---

## 4. 환경 및 현재 코드 상태

### 4-1. 환경

- WSL2 Ubuntu 24.04, VS Code
- 개인 데스크톱 RTX 3060 Ti (8GB), 교육기관 RTX 4060 (8GB)
- `uv` 단일 환경 (conda `intention_env`는 PCPA 제거로 불필요)
- ffmpeg 절대경로 `/usr/bin/ffmpeg`
- GPU 사용률 88%, 처리 속도 실시간 대비 약 5배 (80초 영상 → 10분)

### 4-2. 판정 로직 (변경 없음)

**차량 3단계:**
- `stopped` — 정규화 속도 0.002 미만이 0.35초 이상 → 의무 이행
- `creeping` — 서행하지만 완전 정지 아님 → 위반
- `moving` — 통상 주행 속도 → 위반

**보행자:**
- `crossing` — 폴리곤 안
- `waiting_facing` / `waiting_idle` — 근처 정지 (자세로 구분)
- `approaching` — 정규화 거리 0.6 이내 접근 중
- `leaving` / `near_idle` / `far` — 판정 제외

**최종 판정:** 프레임 단위 교집합. "판정 대상 보행자가 있는 프레임" ∩ "정지하지 않은 차량이 폴리곤 안에 있는 프레임". 트랙 ID를 짝짓지 않으므로 추적이 끊겨도 판정 유지.

**위반 등급 3단계:**
- `high` (위반 가능성 높음) — 차량 `moving` + 보행자 강한 상태(`crossing`/`waiting_facing`)
- `medium` (위반 가능성 보통) — 보행자 강한 상태, 차량은 `creeping`
- `review_needed` (검토 필요) — 보행자가 약한 상태(`approaching`/`waiting_idle`)

### 4-3. 주요 임계값

| 항목 | 값 |
|---|---|
| 차량 완전정지 속도 | 0.002 |
| 차량 서행 상한 | 0.012 |
| 차량 정지 지속 | 0.35초 |
| 차량 진입 겹침 | 2% |
| 보행자 거리 상한 | 1.5 |
| 접근 인정 거리 | 0.6 |
| 추세 윈도우 | 15프레임 |

### 4-4. 검출 설정

```yaml
# my_bytetrack.yaml
track_high_thresh: 0.35
track_low_thresh: 0.1
new_track_thresh: 0.45
track_buffer: 60
match_thresh: 0.8
fuse_score: True
```

```python
# pipeline_core.py:422
classes=[0, 2, 3, 5, 7]   # person, car, motorcycle, bus, truck
conf=0.35
imgsz=1920
# yolo11m.pt + yolo11m-pose.pt
```

### 4-5. 영상 관리

- 영상은 `.gitignore`에 등록해 저장소에 올리지 않음 (GitHub 100MB 제한)
- 녹화: 브라우저 F11 전체화면 → 2560×1440
- crop: `crop=1760:1440:800:0` (왼쪽 어두운 산비탈 제거, 유효 해상도 45% 향상)
  ```bash
  ffmpeg -i 입력.mp4 -vf "crop=1760:1440:800:0" -c:v libx264 -preset fast -crf 20 -an 출력.mp4
  ```
- crop은 선택 사항이다. 자동차 판정은 1404×1014에서도 정상 작동함이 확인됐다. crop의 원래 목적이 오토바이 검출 개선이었으므로, 파인튜닝으로 해결되면 불필요해질 수 있다.
- 자르기: `ffmpeg -ss 시작 -i 입력.mp4 -t 길이 -c copy 출력.mp4`

### 4-6. 진단 스크립트 (저장소에 커밋됨)

| 파일 | 용도 |
|---|---|
| `measure_conf.py` | 영상 전체 클래스별 검출 신뢰도 분포 측정 |
| `check_moto.py` | 특정 프레임에서 conf별 오토바이 검출 확인 |
| `check_moto_visual.py` | 검출 박스를 영상에 그려 시각 확인 (색: 초록≥0.35, 노랑≥0.25, 빨강<0.25) |

이 스크립트들이 임계값 결정의 근거다. 값을 바꿀 일이 생기면 먼저 이걸로 측정한다.

---

## 5. 작업 방식 (유지)

- 코드 수정 시 파일명과 줄 번호, 기존 코드 형태를 함께 제시
- 한 번에 한 곳만 변경
- 값 조정 시 근거를 먼저 제시. 실측 없이 임계값을 정하지 않는다
- 코드에 `...` 같은 생략 표시를 쓰지 않는다
- DECISIONS.md에 기록된 폐기 접근은 다시 제안하지 않는다. 제안하려면 왜 이번엔 다른지 근거를 든다
