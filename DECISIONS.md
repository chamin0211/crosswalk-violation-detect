# 시행착오 기록 (DECISIONS.md)

이미 시도했다가 되돌린 접근들과 그 이유. 같은 길로 다시 들어가지 않기 위한 기록이다.

새 대화에서 작업을 이어갈 때 이 문서를 함께 전달하면, 이미 폐기된 방향을 다시 제안받는 일을 줄일 수 있다.

---

## 판정 원칙

### 실제 횡단 성공을 위반 요건으로 삼지 않는다

**시도했던 것**: "보행자가 실제로 횡단보도를 건넜는가(`crossing` 상태)"를 확인해서, 그 경우에만 위반으로 판정하려 했다.

**왜 폐기했나**: 차가 안 멈추면 사람은 못 건넌다. 횡단 성공을 요건으로 삼으면 **위반이 심할수록 증거가 약해지는 모순**이 생긴다. 차가 그냥 지나가서 보행자가 물러난 경우가 가장 위험한 상황인데, 그 케이스가 오히려 판정에서 빠진다.

**현재 방식**: `approaching`(접근 중), `waiting`(대기 중)도 판정 대상에 포함한다. 도로교통법 제27조의 "통행하려고 하는 때"에 해당하기 때문이다.

---

### `leaving`(멀어짐)을 오탐 지표로 쓰지 않는다

**시도했던 것**: 보행자가 횡단보도에서 멀어지는 궤적을 보이면 "건널 의도가 없었다"고 보고 제외하려 했다.

**왜 폐기했나**: 실제 데이터에서 연석까지 접근했다가(정규화 거리 0.17) 다시 물러난(1.44) 케이스를 확인했는데, 이는 **차가 와서 멈칫하고 물러난 상황**이었다. `leaving`은 위반의 증거일 수 있다.

**현재 방식**: `leaving` 자체를 판정 대상에서 빼지만, 그 앞의 `approaching` 구간은 유지한다.

---

### PCPA 확률을 판정 게이트로 쓰지 않는다

**시도했던 것**: `probability > 0.7`을 위반 판정의 필수 조건으로 걸었다.

**왜 폐기했나**: 실측 결과 대부분의 보행자가 99~100%로 나온다. 횡단보도에서 7m 넘게 떨어져 인도만 걷던 사람도 100%가 나왔다. 변별력이 없는 상태에서 게이트로 쓰면 아무 필터 역할을 못 하면서, "AI가 98% 확률로 판단했다"는 잘못된 신뢰만 준다.

**현재 방식**: 결과에 참고 지표로 표시만 하고, 판정에는 관여하지 않는다. 관측 가능한 근거(거리, 행동, 자세)로 판단한다.

---

## PCPA 포화 진단 (2026-09-06 세션 결과)

### 결론 요약

포화 원인은 **pose 입력 형식 불일치**로 좁혀졌다. 관절 순서와 값 스케일이 학습 데이터와 다르다. box는 아직 판단 보류.

---

### 확정: 모델과 파이프라인 자체는 정상

노이즈 테스트(`~/DPJI/Intention/noise_test.py`) 실측 결과:

| 케이스 | 입력 | 확률 |
|---|---|---|
| 전부 0 | crop/pose/box/speed 모두 0 | 0.410928 |
| 랜덤 | pose·box에 `*1000` 곱한 큰 값 | 0.999992 |
| 정지 | box만 `[1150,420,1180,490]` 고정, 나머지 0 | 0.999989 |

최댓값-최솟값 = 0.589. 입력이 바뀌면 출력이 바뀐다.

따라서 다음은 **문제 없음이 확인됐다**:
- `model.h5` 로딩, npz 읽기
- 입력 순서 `[crop, pose, box, speed]` — 모델 입력명이 `input_1 / input_pose / input_box / input_speed` 순이라 일치
- "모델이 입력을 무시한다"는 가설은 **탈락**

**큰 값이 들어가면 sigmoid가 1.0에 붙는다**는 것이 실측으로 증명됐다.

---

### 확정: 델타 인코딩(`normalize_boxes`)은 완전히 배제

**같은 방향을 다시 제안하지 말 것.**

`configs.yaml`에 `normalize_boxes: true`가 있어서 한때 유력 후보였다. 실제 구현은 `action_predict.py:768-774`:

```python
if normalize:
    for k in d.keys():
        if k != 'tte' and k != category_name:
            if k != 'box' and k != 'center':
                for i in range(len(d[k])):
                    d[k][i] = d[k][i][1:]
            else:
                for i in range(len(d[k])):
                    d[k][i] = np.subtract(d[k][i][1:], d[k][i][0]).tolist()
```

이름과 달리 "이미지 크기로 나누기"가 아니라 **첫 프레임 좌표를 빼는 델타 인코딩**이다. 그리고 시퀀스 길이가 16 → 15로 줄어든다.

**배제 근거 2가지:**

1. 모델 입력 shape이 전부 시간축 16이다.
   ```
   input_1     (None, 16, 112, 112, 3)
   input_pose  (None, 16, 34)
   input_box   (None, 16, 4)
   input_speed (None, 16, 1)
   ```
   델타를 썼다면 15여야 한다.

2. PCPA 클래스(`action_predict.py:4747-4980`) 내부에서 config를 강제로 덮어쓴다.
   ```python
   # 4791-4792 (get_data_sequence 계열)
   assert model_opts['obs_length'] == 16
   model_opts['normalize_boxes'] = False

   # 4855-4856 (get_data)
   assert model_opts['obs_length'] == 16
   model_opts['normalize_boxes'] = False
   ```

config에 뭐라 적혀 있든 PCPA는 델타를 쓰지 않는다. 문서상 `true`는 무의미하다.

---

### 확정: pose 관절 순서가 다르다

학습 데이터 pose 포맷 (`utils.py:111-112` 주석):
```
[nose, neck, Rsho, Relb, Rwri, Lsho, Lelb, Lwri, Rhip, Rkne,
 Rank, Lhip, Lkne, Lank, Leye, Reye, Lear, Rear, pt19]
```
→ **OpenPose 계열 순서**

우리가 넣는 것 (`pipeline_core.py` `_match_pose_to_box`):
```python
kpts = pose_result.keypoints.xy[best_idx].flatten().tolist()
```
→ **YOLO11-pose의 COCO-17 순서**, 픽셀 좌표, 정규화 없음

| 인덱스 | 학습 (OpenPose) | 우리 (COCO-17) |
|---|---|---|
| 0 | nose | nose |
| 1 | **neck** | left_eye |
| 2 | Rsho | right_eye |
| 3 | Relb | left_ear |
| 4 | Rwri | right_ear |
| 5 | Lsho | left_shoulder |
| ... | ... | ... |
| 14 | Leye | right_knee |
| 15 | Reye | left_ankle |
| 16 | Lear | right_ankle |

주요 차이:
- OpenPose에는 `neck`(양 어깨 중점)이 있고 COCO-17에는 없다
- 얼굴 관절 위치가 다르다 (OpenPose는 뒤쪽 14~17, COCO는 앞쪽 1~4)
- shape이 `(16, 34)`로 우연히 맞아서 에러가 안 났을 뿐, 내용은 뒤죽박죽

---

### 확정: 학습 pose는 0~1 정규화 값

`data/features/iddp/poses/pose_gp_set_0003_iddp_body2d.pkl` 실측:

```
pose 길이: 34 (17관절 x 2, 19관절 아님)
최솟값: 0.0
최댓값: 0.807
x좌표 범위: 0.0 ~ 0.383
y좌표 범위: 0.0 ~ 0.808
```

샘플 100개 분포:
```
x 최댓값들의 평균: 0.606   최대: 0.739
y 최댓값들의 평균: 0.822   최대: 0.983
```

우리는 픽셀 좌표(1150, 420 등)를 넣고 있다. **수천 배 차이.**

pkl 구조: `{vid_id: {frame_pedid: [34개 float]}}`. 값이 없는 프레임은 `[0]*34`로 채워진다.

---

### 미확정: pose 정규화의 기준

**추측으로 정하지 말 것.** 위 분포가 어느 쪽으로도 깔끔하게 설명되지 않는다.

- **박스 기준**이라면 x, y 모두 1.0 근처가 자주 나와야 하는데 x 최대가 0.739뿐이다
- **이미지 크기 기준**이라면 사람 한 명이 화면 세로 82%를 평균적으로 차지한다는 뜻이라 비현실적이다

가능한 설명 (전부 미검증):
1. `enlarge_ratio: 1.5` — 박스를 1.5배 키운 뒤 정규화하면 사람이 차지하는 비율이 줄어든다
2. `squarify` (`utils.py:199`) — 세로로 긴 박스를 정사각형으로 만들면 x 비율이 작아진다
3. 우리가 연 pkl은 `iddp` 데이터셋 것인데 체크포인트는 PIE 학습이다. 데이터셋별 처리가 다를 수 있다

**다음 확인:** pose pkl을 *만드는* 코드를 찾아야 한다. 지금까지 본 `get_pose`는 읽는 쪽이다.
```bash
grep -rn "body2d\|openpose\|pose_gp" ~/DPJI/Intention/*.py | head -20
```

---

### 미확정: box도 정규화가 필요한가

모순이 남아 있다.

- `action_predict.py:4826-4828`에서 box는 가공 없이 통과한다.
  ```python
  else:
      features = data[d_type]
      feat_shape = features.shape[1:]
  ```
  `obs_input_type`의 `box`, `speed`는 `local`/`context`/`mask`/`pose` 어디에도 안 걸려서 이 `else`로 떨어진다.

- 그런데 노이즈 테스트 케이스 3에서 box만 픽셀 좌표로 넣었더니 0.99999가 나왔다.

가능성:
1. PIE raw data 단계에서 `bbox`가 이미 정규화된 상태로 저장돼 있다
2. box는 픽셀 좌표가 맞고, 케이스 3의 포화는 다른 이유다

**pose를 먼저 고치고 재측정해서 판단한다.** 두 개를 동시에 바꾸면 원인 분리가 안 된다.

---

### 미확정: crop 전처리

`action_predict.py:51`에 `from tensorflow.keras.applications.vgg19 import preprocess_input`이 있고 381, 420번째 줄에서 쓰인다. 하지만 PCPA의 backbone은 `c3d`라 이 경로를 타는지 확인되지 않았다.

현재 우리는 `/255.0`만 적용 중(`predict_from_npz_cli.py:28`). pose 문제 해결 후에 다룬다.

---

### 별도로 남은 문제: speed 입력

PIE는 차량 탑재 카메라 데이터셋이라 `speed`가 **촬영 차량의 OBD 주행 속도**다. 우리는 고정 CCTV라 그런 값이 존재하지 않는다.

현재 `predict_from_npz_cli.py:31`에서 전부 0으로 채운다:
```python
speed = np.zeros((SEQ_LEN, 1), dtype=np.float32)
```

형식은 맞지만 의미가 없는 입력이다. 도메인 자체가 다른 문제라 전처리 수정으로는 해결되지 않는다. 이 모델을 고정 CCTV에 쓰는 것이 타당한지 자체를 재검토할 필요가 있다.

---

### 별도로 남은 문제: 관찰 윈도우 위치

현재는 트랙의 **마지막 32프레임**을 사용한다(`pop(0)`으로 앞을 버림). 이미 건넌 뒤의 프레임이 포함되면 "의도 예측"이 아니라 "횡단 여부 사후 확인"이 된다.

PCPA는 원래 횡단 시작 시점(TTE, time-to-event) 기준으로 역산한 관찰 윈도우를 쓴다. 로직 2에서 만든 `t_ready`(대기/접근 상태 시작 시점)를 기준으로 그 **이전** 구간을 넣어야 원래 취지에 맞다.

이건 입력 형식 문제를 해결한 뒤에 따로 다룬다.

---

### 진단용 스크립트 (재사용 가능)

세션 중 만든 것들. `~/DPJI/Intention/`에 있다.

- `check_input_shape.py` — 모델이 기대하는 입력 개수, 이름, shape 출력
- `noise_test.py` — 가짜 입력 3종으로 모델 반응 확인
- `check_pose_pkl.py` — 학습 pose pkl의 값 범위와 분포 확인

실행은 반드시 절대경로로:
```bash
/home/mingyu/miniconda3/envs/intention_env/bin/python \
  /home/mingyu/DPJI/Intention/<스크립트명>.py
```

---

## 검출·추적

### 임계값 조정만으로 야간 검출 문제를 풀 수 없다

**시도했던 것**: `track_high_thresh`, `new_track_thresh`, `conf`를 올렸다 내렸다 하며 최적값을 찾으려 했다.

**결과**: 올리면(0.5/0.6) 야간에 사람이 통째로 안 잡혀 후반부 검출이 전무해졌고, 내리면(0.3/0.4) 실제 2명인 장면에서 11개 트랙이 생겼다. 트레이드오프만 있고 해결이 없었다.

**현재 값**: `track_high_thresh: 0.35`, `new_track_thresh: 0.45`, `conf: 0.35`. 실측 conf 분포(사람 0.32~0.79)를 보고 정한 값이다.

**주의**: 이 값들은 야간 CCTV 기준이다. 다른 도메인에서는 다시 맞춰야 한다. 값 조정은 근본 해결이 아니라는 점을 기억할 것.

---

### 모델을 키워도 뭉개진 영역은 못 잡는다

**시도했던 것**: `yolo11s` → `yolo11m`으로 교체, `imgsz` 1280 → 1920으로 상향.

**결과**: 트랙 수는 늘었지만, 조명 대비가 극단적인 구간(횡단보도 위 과노출 + 인도 어두움)에서는 여전히 검출이 끊겼다. 입력 이미지에 정보 자체가 없으면 모델 크기로 해결되지 않는다.

**미시도**: CLAHE 전처리. LAB 색공간의 L 채널에만 적용해 영역별 대비를 보정하는 방식. 검출용 프레임에만 적용하고 렌더링에는 원본을 써야 한다.

---

### 트랙 병합(track linking)은 공백이 클 때 위험하다

**시도하려 했던 것**: 가림으로 조각난 트랙들을 위치 예측으로 이어붙이고 사이를 보간하려 했다.

**왜 보류했나**: 실제 데이터에서 보행자 트랙 사이 공백이 99프레임(3.3초)이었다. 차량은 관성이 있어 3초 보간도 비교적 안전하지만, **사람은 3.3초면 5m를 움직이고 방향도 바꿀 수 있다**. 관측되지 않은 구간을 추정으로 채워 위반 판정에 쓰면 근거가 약해진다.

**대신 한 것**: 프레임 단위 판정으로 전환. 특정 보행자와 특정 차량을 짝지어 비교하지 않고, "그 프레임에 판정 대상 보행자가 있었는가"와 "그 프레임에 정지 안 한 차가 있었는가"의 교집합을 본다. 트랙이 조각나도 판정이 유지된다.

**차량 쪽은 여지가 있음**: 같은 트랙 내부 공백이 최대 15프레임(0.5초) 정도라 보간이 안전하다. 다만 정지 판정은 관측 프레임만 써야 한다.

---

### `track_buffer`를 크게 늘리는 것은 해결책이 아니다

**시도하려 했던 것**: 가림 구간을 메우려고 `track_buffer`를 150(5초)까지 올리려 했다.

**왜 폐기했나**: buffer는 트랙을 살려둘 뿐이고, 재등장 시 같은 트랙으로 이을지는 `match_thresh`(IoU 기반)가 결정한다. 가려진 동안 차가 이동했으면 위치가 크게 달라져 IoU가 안 맞는다. 그리고 5초는 다른 객체에 ID가 잘못 붙을 위험이 크다.

**현재 값**: `track_buffer: 60` (2초).

---

### `yolo.track(source=video_path)` 대신 프레임을 직접 넘긴다

**있었던 의심**: YOLO가 내부적으로 영상을 읽고 우리가 `cap.read()`로 또 읽으면 두 스트림이 어긋날 수 있다고 봤다.

**결과**: 구조를 바꿔도 결과가 동일했다. 동기화는 원래 어긋나지 않고 있었다. 다만 프레임을 직접 넘기는 쪽이 더 명시적이고 안전하므로 현재 구조를 유지한다.

---

## 보행자 상태 판별

### 이동 방향(벡터 각도)으로 인도 통행자를 거르지 않는다

**시도했던 것**: 보행자의 이동 방향과 횡단보도 긴 축이 이루는 각도가 60도 이내일 때만 `approaching`으로 인정했다.

**왜 폐기했나**: 실제로 횡단보도로 접근하던 사람이 걸러졌다. 인도가 횡단보도와 비스듬하게 놓여 있으면, 건너러 가는 중에도 이동 방향이 기준을 못 넘는다. 15프레임 윈도우로 재면 대부분의 시간 동안 "인도를 따라 걷는 방향"이 잡히고, 방향을 트는 건 마지막 순간뿐인데 그때는 이미 폴리곤에 진입해 `crossing`이 된다.

**대신 한 것**: 거리 조건 강화. `approaching`으로 인정하려면 정규화 거리 0.6 이내(약 1m, 연석 근접)여야 한다. 거리 추세는 방향 벡터보다 목적지 의도를 직접 반영한다.

**근거 데이터**: 실제 인도 통행자들은 최소 거리가 2.1~5.1이었고, 건너려던 사람은 0.6 이하로 들어왔다. 중간값이 거의 없어 구분이 명확했다.

---

### 후보 선정 시 "폴리곤 안에 들어온 적 있는가"로 거르면 안 된다

**있었던 버그**: `near_zone_score`가 `in_zone(cx, cy)`, 즉 폴리곤 내부 진입 여부로 후보를 필터링했다. 이 때문에 **연석에 서 있다가 차 때문에 못 건넌 보행자가 후보 단계에서 통째로 제외**됐다. 로직 2에서 `waiting`, `approaching` 상태를 만들어놨는데 정작 그 상태에 도달할 사람이 걸러지고 있었다.

**수정**: 발 위치 기준 정규화 거리 2.0 이내면 후보로 삼는다. 후보 선정은 넉넉하게, 실제 판정은 엄격하게.

**영향**: 이 수정 후 보행자 후보가 3명 → 10명으로 늘었고, `approaching` 상태가 처음으로 정상 검출되기 시작했다.

---

### 대기 시간으로 판별하지 않는다

**시도하려 했던 것**: "연석 근처에 1초 이상 머물면 대기 중"으로 판정하려 했다.

**왜 폐기했나**: 비신호 횡단보도에서는 차가 없으면 사람이 멈추지 않고 그냥 건넌다. 신호를 기다리는 상황이 아니므로 "대기"라는 상태가 짧거나 아예 없다. 실제로 `waiting_facing`/`waiting_idle` 상태가 한 번도 나오지 않았다.

---

## 좌표·기하

### 폴리곤 좌표 순서는 반드시 정렬해야 한다

**있었던 문제**: 사용자가 클릭 순서를 틀리면 폴리곤이 X자(나비넥타이)로 꼬인다. 이 상태에서는 `pointPolygonTest`의 내부 판정이 무너지고 면적 겹침 계산이 왜곡된다. 실제로 `max_overlap`이 0.228로 잘못 나와 엉뚱한 위반이 잡혔다.

**해결**: `_order_polygon_points`로 무게중심 기준 각도 정렬. 중심 주위를 한 바퀴 도는 순서가 되므로 어떤 입력 순서든 볼록 사각형이 보장된다. `run_pipeline` 첫 줄에서 적용한다.

**정렬 후**: `max_overlap`이 0.228 → 1.0으로 정상화됐다.

---

### 위치 기준점은 중심점이 아니라 접지점을 쓴다

**차량**: 박스 하단 중앙(`(x1+x2)/2, y2`). 중심점은 공중에 떠 있어 박스 높이 변동에 흔들린다.

**보행자**: 박스 하단 중앙(발 위치). 중심점은 상체 높이라 실제 서 있는 위치보다 도로 안쪽으로 치우친다.

---

### 원근 보정은 대상 자신의 크기로 정규화한다

픽셀 거리·속도를 그대로 쓰면 화면 위쪽(먼 곳)과 아래쪽(가까운 곳)의 의미가 달라진다.

- **차량 속도**: 프레임 간 이동거리 ÷ 박스 대각선
- **보행자 거리**: 폴리곤까지 거리 ÷ 박스 높이 (박스 높이 ≈ 그 위치에서의 1.7m)

호모그래피나 실거리 스케일 추정 없이 이 방식으로 충분히 작동한다. 사용자에게 횡단보도 실제 치수를 입력받는 방식은 실용성이 없어 폐기했다.

---

## 정지 판정

### 최소 속도(`min_speed`) 하나로 정지를 판정하면 안 된다

**있었던 버그**: `min_speed < 5.0`이면 정지로 판정했다. 구간 내 최솟값 하나만 보므로 **추적 흔들림 한 프레임에 정지로 오판**됐다. 평균 속도 5.34로 계속 달리던 차가 최솟값 0.12 때문에 "정지함"으로 분류되어 위반에서 빠졌다.

**수정**: "정규화 속도 0.002 미만이 0.35초 이상 연속 지속"을 요구한다. 노이즈는 걸러지고 실제 정지 동작만 잡힌다.

---

### 서행을 정지로 인정하면 안 된다

도로교통법상 "일시정지"는 완전 정지를 의미한다. 서행은 의무 이행이 아니다. 그래서 `stopped` / `creeping` / `moving` 3단계로 구분하고, `creeping`도 위반 후보에 포함한다.

임계값 0.012(시속 약 6km)로 잡으면 크리핑 수준의 저속 주행이 `creeping`으로 분류된다. 0.002 미만(시속 약 1km)만 완전 정지로 본다.

---

### 횡단보도 진입은 중심점이 아니라 면적 겹침으로 판단한다

**있었던 문제**: 박스 중심점이 폴리곤 안에 있는지로 판단했다. 차가 대각선으로 찍히면 앞범퍼가 횡단보도를 침범해도 중심점은 밖에 있어 미검출됐다. 위반을 놓치는 방향의 오류라 단속 관점에서 치명적이다.

**수정**: 박스와 폴리곤의 실제 겹침 면적 비율이 2% 이상이면 진입으로 본다.

---

## 증거 제시

### 스냅샷은 차량 진입 시점을 기준으로 뽑는다

**시도했던 것**: `crossing` 상태인 가장 이른 프레임을 대표 이미지로 골랐다.

**왜 바꿨나**: 보행자가 횡단보도에 발을 딛는 순간을 잡으니, 그때는 차가 이미 다 지나간 뒤였다. "차가 안 멈추고 지나갔다"는 증거로는 약했다.

**현재 방식**: 그 차량의 폴리곤 진입 프레임(`zone_frames` 중 위반 구간의 첫 프레임)과 통과 완료 프레임 두 장을 뽑는다. 진입 시점에 보행자가 이미 대기/접근 중이었다는 것이 핵심 근거다. UI에서 라디오 버튼으로 두 시점을 전환한다.

---

### 위반 이벤트는 차량 단위로 묶는다

**시도했던 것**: 프레임 간격이 15프레임 이상 벌어지면 별개 이벤트로 분리했다.

**왜 바꿨나**: 같은 차량이 같은 횡단보도를 한 번 통과하는 상황인데, 중간에 검출이 끊겨 2건으로 쪼개졌다. 실무적으로 차 한 대가 횡단보도를 한 번 지나가는 것이 위반 1건이다.

**현재 방식**: 같은 차량 ID의 위반 프레임을 하나로 묶고, `severity`는 가장 높은 등급을 채택한다.

---

## UI

### Streamlit wide 레이아웃에서는 콘텐츠를 가운데로 모아야 한다

화면 폭이 넓은데 콘텐츠가 좌우 컬럼으로 나뉘면 가운데가 텅 빈다. `st.columns([1, 5, 1])`처럼 바깥 여백 컬럼으로 감싸 해결한다.

### 결과는 별도 화면으로 전환한다

업로드 UI와 결과가 한 페이지에 세로로 쌓이면 스크롤이 길어진다. `st.session_state.page`로 `upload` / `analyzing` / `result` 3단계를 나누고 `st.rerun()`으로 전환한다.

### 스냅샷은 세로로 쌓지 않는다

이벤트가 여러 건이면 라디오 버튼으로 하나를 선택해 크게 보는 방식이 낫다. 전부 나열하면 화면 밖으로 밀려난다.

### 상세 데이터는 원시 JSON이 아니라 표와 서술로 보여준다

검토자에게 `min_norm_speed: 0.00041` 같은 값은 의미가 없다. 한글 라벨(`moving` → "정지하지 않고 통과")과 표, 그리고 판단 근거를 문장으로 제시한다. 개발용 원시 데이터는 별도 탭에 접어둔다.

### 클릭한 좌표는 화면에 표시한다

점을 찍어도 표시가 없으면 사용자가 어디를 클릭했는지 모른다. PIL `ImageDraw`로 점과 연결선을 그리고, 4점이 모이면 자동 정렬된 순서로 사각형을 미리 보여준다.

---

## 기타 환경

- `conda run` 명령은 PATH 문제로 실패한다. conda 환경 파이썬은 **절대경로로 직접 호출**한다.
- ffmpeg는 uv 환경 PATH에 없으므로 `/usr/bin/ffmpeg` 절대경로로 호출한다.
- 파이썬 코드를 터미널에 직접 붙여넣지 않는다. 코드는 파일에, 터미널에는 실행 명령어만.
- `cv2.putText`는 한글을 그리지 못할 수 있다. 스냅샷 헤더는 영문으로 작성한다.
- 사고 영상(실제 인명 피해가 발생한 장면)은 윤리적 이유로 테스트와 발표 자료에서 배제한다.
