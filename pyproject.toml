[project]
name = "crosswalk-violation-detect"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chamin0211", email = "mk.cha2002@gmail.com" }
]
requires-python = ">=3.12,<3.13"
dependencies = [
    "streamlit>=1.63.0",
    "streamlit-image-coordinates>=0.4.1",
    "torch==2.5.1",
    "torchvision==0.20.1",
    "ultralytics>=8.4.138",
]

[project.scripts]
crosswalk-violation-detect = "crosswalk_violation_detect:main"

[[tool.uv.index]]
url = "https://download.pytorch.org/whl/cu121"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
